# ── Core LangChain stack ──────────────────────────────────────────────────────
langchain
langchain-community
langchain-core
langchain-huggingface
langchain-groq
huggingface-hub

# ── ML / AI libraries ─────────────────────────────────────────────────────────
# sentence-transformers: Bi-encoder models for semantic similarity & embeddings
#   Used in: FAISS vector store (RAG pipeline) + Interview Guider (PyTorch cosine scoring)
sentence-transformers

# torch: PyTorch deep learning framework (pulled in by sentence-transformers)
#   Used directly in interview_guide.py:
#     - model.encode(..., convert_to_tensor=True) → torch.Tensor (shape: [2, 384])
#     - torch.dot(embeddings[0], embeddings[1])   → cosine similarity scalar
#   Explicit listing makes the ML dependency visible and version-pinnable.
torch

# faiss-cpu: Facebook AI Similarity Search — vector index for RAG retrieval
#   Used in: helper.py — stores and searches document chunk embeddings
faiss-cpu

# ── LLM API ───────────────────────────────────────────────────────────────────
groq                # Groq SDK — direct streaming + structured JSON calls (stock_chat.py)

# ── Web framework ─────────────────────────────────────────────────────────────
streamlit

# ── Document processing ───────────────────────────────────────────────────────
pypdf               # PDF text extraction (resume_analyzer.py, helper.py)

# ── Authentication ────────────────────────────────────────────────────────────
# bcrypt: password hashing (work factor 12 by default — slow enough to resist brute-force)
#   Used in: src/auth.py — register_user(), login_user(), change_password()
bcrypt

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv       # .env file loading

# ── Stock / Jobs modules ──────────────────────────────────────────────────────
requests
beautifulsoup4
lxml
pandas

# ── Agentic AI — REST API layer ───────────────────────────────────────────────
# fastapi: ASGI web framework with automatic OpenAPI 3.0 generation
#   Used in: api/main.py — exposes the ReAct agent as a REST API
fastapi

# uvicorn: ASGI server that serves the FastAPI application
#   Run with: uvicorn api.main:app --reload --port 8000
uvicorn[standard]

# duckduckgo-search: DuckDuckGo web search (no API key required)
#   Used in: src/agent_tools.py — _tool_search_web() (search_web agent tool)
duckduckgo-search

-e .
